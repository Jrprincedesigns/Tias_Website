# Node — the theme itself has no build step; this covers spa-app/
node_modules/
.env
.env.*
!.env.example

# Supabase CLI scratch. Patterns with a slash anchor to this file's directory,
# so the bare form missed spa-app/supabase/.temp/ when the CLI was run there.
**/supabase/.temp/
**/supabase/.branches/

# Vercel CLI / build-preset scratch
**/.vercel/

# OS noise
.DS_Store
